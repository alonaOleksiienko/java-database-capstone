# ================================
# Application Configuration
# ================================
spring.application.name=back-end

# ================================
# MySQL Database Configuration
# ================================
spring.datasource.url=jdbc:mysql://localhost:3306/cms?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=328111
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ================================
# MongoDB Configuration (optional)
# Uncomment if MongoDB is running locally.
# Otherwise, disable Mongo auto-config below.
# ================================
# spring.data.mongodb.uri=mongodb://root:YOUR_PASSWORD@localhost:27017/prescriptions?authSource=admin

# If MongoDB is NOT in use yet:
spring.autoconfigure.exclude=\
org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration,\
org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration

# ================================
# Actuator / Health
# ================================
management.endpoint.health.show-details=always
management.health.db.enabled=true

# ================================
# Security / JWT
# ================================
api.path=/
jwt.secret=$!@#$^%$$$%####$DDCPN0234FCFDPD8670M

# ================================
# Static Resources & UI
# ================================
spring.web.resources.static-locations=classpath:/static/

# ================================
# Thymeleaf Configuration
# ================================
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false
